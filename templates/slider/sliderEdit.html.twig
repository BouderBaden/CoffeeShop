{% extends '../base.html.twig' %}

{% block title %}Slider | Ajout{% endblock %}


{% block body %}
    <div class="flex justify-center ">
        {{ form_start(form) }}
        {{ form_rest(form) }}
        <button class="bg-emerald-950">Submit</button>
        {{ form_end(form) }}
    </div>

{% endblock %}